Round (or rounded-square) avatar; shows the image, else colored initials.

```jsx
<Avatar name="Priya Karthik" size="lg" />
<Avatar src="/photo.jpg" name="Mia" shape="rounded" />
```

Fallback color is derived from the name; override with `tone`.
