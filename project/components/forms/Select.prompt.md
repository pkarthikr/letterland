Dropdown for choosing one option from a list. Pass `options` as strings or `{value,label}`.

```jsx
<Select label="Child's age" placeholder="Select age"
  options={['3 years', '4 years', '5 years', '6 years']} />
```

Shows the brand chevron and matches Input styling. `error` overrides `helper`.
